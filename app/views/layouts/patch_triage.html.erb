<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tag %>
  <%- if SiteSetting.site_favicon_url.present? %>
  <link rel="icon" type="image/png" href="<%= SiteSetting.site_favicon_url %>">
  <%- end %>
  <title><%= yield(:title).presence || "Patch Triage" %></title>
  <link rel="stylesheet" href="/plugins/blog/blog.css?v=34">
  <link rel="stylesheet" href="/plugins/blog/patch-triage.css?v=9">
  <script src="/plugins/blog/patch-triage.js?v=6" nonce="<%= csp_nonce_placeholder %>"></script>
</head>

<body class="patch-triage-page">
  <div id="wrapper">
    <header class="hon-header">
      <a href="/patch-triage" class="hon-logo">Patch Triage</a>
      <nav class="hon-nav">
        <a href="/patch-triage" class="<%= 'active' if request.path =~ %r{^/patch-triage(/\d+)?$} %>">Rate</a>
        <a href="/patch-triage/list" class="<%= 'active' if request.path == '/patch-triage/list' %>">List</a>
        <a href="/patch-triage/leaderboard" class="<%= 'active' if request.path.start_with?('/patch-triage/leaderboard', '/patch-triage/by/') %>">Leaderboard</a>
        <a href="/patch-triage/stats" class="<%= 'active' if request.path == '/patch-triage/stats' %>">Stats</a>
        <% if current_user %>
        <a href="/patch-triage/my-patches" class="<%= 'active' if request.path == '/patch-triage/my-patches' %>"><%= current_user.username.capitalize %></a>
        <% end %>
        <% if current_user&.admin? %>
        <a href="/patch-triage/admin/patches" class="<%= 'active' if request.path.start_with?('/patch-triage/admin') %>">Admin</a>
        <% end %>
        <a href="/" class="hon-back">Forum</a>
      </nav>
    </header>
    <main class="hon-main">
      <% if flash[:notice] %>
        <div class="flash notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="flash alert"><%= flash[:alert] %></div>
      <% end %>
      <% if flash[:warning] %>
        <div class="flash warning"><%= flash[:warning] %></div>
      <% end %>
      <%= yield %>
    </main>
  </div>
</body>
</html>
